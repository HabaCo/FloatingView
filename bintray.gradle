apply plugin: 'com.novoda.bintray-release' // must be applied after your artifact generating plugin (eg. java / com.android.library)

publish {
    repoName = "Android-Lib"
    userOrg = "habaco"
    groupId = "github.habaco.view"
    artifactId = 'view-floating'
    publishVersion = '1.0.0'
    desc = 'A simple util to generate floating view with default style or simply custom function'
    website = "https://github.com/HabaCo/FloatingView"
}
